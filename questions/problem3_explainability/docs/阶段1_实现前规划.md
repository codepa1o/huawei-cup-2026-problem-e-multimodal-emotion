# 阶段1：统一输入与冻结预测

需求：复用三种子等权概率／强度集成，文本固定BERT，A/V固定训练标准化，50个位置不移动。

实现：独立FrozenPredictor；验证vendor与BERT哈希；编码前干预，按实际token输入缓存；读问题二缓存须核对清单与样本ID；无扰动批次对比原FeatureDataset＋Ensemble；人工遮挡不得还原已有不可用位置。

验收：3条训练样本的重新编码、缓存输入和预测一致，类别完全一致；全空走原先验回退。推理只读，不训练、不重新拟合参数。
