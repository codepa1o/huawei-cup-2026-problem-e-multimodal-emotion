# 阶段1—2运行报告

阶段1：3条训练样本内容长度为1/20/48。缓存路径与原模型概率最大差异2.98e-8；重编码路径最大差异2.09e-7，均通过atol=1e-6、rtol=1e-5输出核验且类别一致。BERT不同批量的float32计算有舍入差异，编码层最大绝对差异2.67e-5，在atol=1e-5、rtol=1e-5的联合门槛内；没有修改模型参数。全空先验回退通过。

阶段2：实现token字符偏移、词组、截断词、给定转写强制对齐、真实帧PTS与音频偏移侧车。10项边界测试通过。附件4尚未执行音视频对齐；自动时间仍需人工核验，未将状态设成人工完成。
