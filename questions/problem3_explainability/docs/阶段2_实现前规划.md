# 阶段2：证据坐标与时间映射

先实现原位置→字符→完整词组的无泄漏映射，保留标点、重复词、截断词状态。用固定BERT三路输入逐值校对；时间采用视频首PTS原点、音频起始偏移、给定转写强制对齐。官方A/V行只能借词时间作近似锚点，不能声称恢复了官方特征提取的真实毫秒边界。

本阶段用合成边界测试开发定位逻辑；附件4全量音视频解析在阶段6规则冻结后执行。不能以其卡片好坏调模型。OOV、无匹配、短异常词段和越界均显式失败／警告，不套kT/50填时间；人工核验初始pending。
