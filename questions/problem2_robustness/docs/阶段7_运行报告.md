# 阶段7：附件3专项推理与交付运行报告

运行日期：2026-09-23。状态：**已完成并通过独立验收**。

产物根目录：`outputs/stage7/c5dd1e48a432b547/`。冻结来源为阶段6的M1-uniform三种子等权概率／强度集成。未重新训练、调阈值或使用附件3选择模型。

## 1. 执行结果

| 项目 | 实测结果 |
|---|---:|
| 官方对齐文件／预测记录 | 30／30 |
| 负向／中性／正向预测 | 5／13／12 |
| 全空先验回退 | 0 |
| 存在未知支持位置的样本 | 29 |
| 严格分类—强度符号冲突 | 14 |
| 单元测试 | 75项通过 |
| 原始权重／轻量权重／解包重跑最大差异 | 0.0 |
| 问题二ZIP字节数 | 2,378,830（十进制约2.38 MB） |
| 包内清单文件数 | 94（另有清单自身） |

14条严格冲突中，13条是中性类别对应非零回归值，另1条为`附件3_24.pkl`分类Negative但回归值为正。该统计沿用冻结前的“严格符号”口径，不等于14条已知错误。附件3没有标签，无法判定预测正误，也不能报告其Accuracy/F1/MAE/Pearson。没有对专项结果进行事后类别覆盖、强度置零或阈值调整。

“未知支持位置”表示全零可能来自缺失，也可能来自padding，不能还原真实有效长度。29条未知状态不等于29条异常失败；原始观测保持50个官方序列位置。

## 2. 正式产物

- `predictions/附件3_情感预测结果.csv`：正式最小四列，文件名、文件内0起始行号、极性、强度。
- `predictions/附件3_预测审计明细.csv`：增加概率、实际可观测数量、未知支持数量、回退／冲突及模型来源。
- `predictions/predictions_unrounded.json`：保留未舍入概率和强度，供复验。
- `original_checkpoint_reference/`：三份原始完整checkpoint的独立前向结果。
- `package/`与`问题二_附件3推理交付包.zip`：轻量部署参数、训练集normalizer、源码、测试、配置、依赖快照、主要分析结果、14幅PNG/SVG及中文复现说明。
- `unpacked_replay/`、`replay_predictions/`、`replay_stdout.log`：从ZIP解压的新副本重新编码和推理30条的实录。
- `validation_report.json`、`implementation_manifest.json`、`tests.log`：最终验收、源码指纹与测试结果。

CSV为UTF-8 BOM，小数保留6位；未舍入JSON校验概率行和、argmax、[-3,3]范围；导出舍入误差限制5.1e-7。复验按atol=1e-6、rtol=1e-5，实际概率和强度差均为0，类别完全一致。

ZIP SHA256：`261f8f476fb2fc291344737315b8c38ef3cdd339f7b7b98e391fa9b888ff916f`。

## 3. 关键实现

- `predict_attachment3.py`：独立无标签推理，先核对官方文件和部署权重哈希，再复用冻结的预处理。禁止用raw_text或非对齐版补信息。
- `delivery_utils.py`：阶段6前置放行、包内路径校验、文件清单、禁止覆盖的安全解压。
- `deliver_attachment3.py`：只保留模型结构／参数，不含优化器和随机流；构建包并在解压工作目录运行。
- `validate_delivery.py`：独立重建掩码、比对权重张量、预测字段、原始文件、ZIP与清单、源码一致性、无标签和旧阶段绑定。
- `tests/test_delivery.py`：路径越界、非法数值、舍入、清单篡改、危险ZIP和禁止覆盖等边界。

新增代码有中文注释，解释掩码语义、使用范围、舍入与失败规则。Ruff检查通过。阶段0—6已冻结源文件和绑定产物未改动，问题一既有未提交修改没有被回退或纳入本阶段处理。

首次通过的交付版本为82文件；随后补齐包内测试源码和显式代码检查，重新制作94文件版本并重新解包编码、复验。首次版本保留在同级`c5dd1e48a432b547_initial_verified/`，没有删除。最终以`latest_run.json`及本报告目录为准。

## 4. 可执行命令

在问题二工程目录中：

```powershell
$env:PYTHONUTF8 = '1'
.venv/Scripts/python.exe -m src.deliver_attachment3
.venv/Scripts/python.exe -m src.validate_delivery
```

已完成的交付入口先检查并复用现有包；不是每次都重写实验。需要在独立环境复现时解压ZIP，按包内README安装依赖、准备固定BERT缓存，再执行`src.predict_attachment3`，指定实际官方输入目录和包外新输出目录。

## 5. 尚未覆盖的事项

1. ZIP不包含官方附件、虚拟环境、训练缓存及大型通用BERT权重。首次获取BERT需要联网，revision与逐文件SHA256固定；不能声称不含权重也能直接离线运行。
2. 本次解包复验使用本机已有CPU环境和固定BERT缓存，不是全新机器的离线安装试验。
3. 本阶段仅完成问题二子包检查。**全题50MB仍需合并问题一／二／三最终材料后另验**；本包小于50MB不代表全题提交已达标。
4. 已检查包内本机用户／项目绝对路径泄露；论文、全题文件、身份匿名性仍需最终提交审计。
5. 问题二必要计算主线已完成。接下来可写问题二论文章节；问题三贡献分解、证据定位与验证尚需单独实现，不能把融合权重直接当因果解释。
