# 问题一阶段0—2运行报告

本报告对应当前工程的实际运行结果。数据输入为附件1的100条原始视频及 `label-100.xlsx`，未改写原始附件；输出均在工程的 `outputs/` 目录。配置、依赖版本与模型校验值见 `project.toml`、`requirements.txt` 和各阶段的 `environment.json`。

## 完成情况

| 阶段 | 实际结果 | 主要产物 |
|---|---|---|
| 0 样本清单 | 100条标签、100个唯一样本ID、100个MP4，全部可探测；0错误、2提示 | `outputs/stage0/manifest.csv`、`summary.json`、`issues.jsonl` |
| 1 原始时间轴 | 100条均有视频帧、音频帧、转写词时间索引；完整词级70条、部分词级15条、仅片段级15条；0失败 | `outputs/stage1/timelines/`、`status.csv` |
| 2 原始特征 | 100个NPZ，0提取失败；文本词向量768维、音频帧18维、视觉采样帧628维；87条状态正常，13条待复核 | `outputs/stage2/features_raw/`、`feature_index.csv` |
| 联合校验 | 100个ID与特征文件全部通过结构和时间契约检查；0错误、7提示 | `outputs/validation/report.json`、`summary_100.csv` |

阶段1共1932个转写词，其中1664个获得词级起止时间。未获得词时间的部分明确保留缺失掩码和 `-1` 哨兵；不能把句段范围或均匀分配时间当作真实词对齐。阶段2另存整段文本向量，100条均成功；其 `[0,T]` 只表示所属视频片段，并不赋予词级时间精度。

视觉按5 Hz从实际存在的视频帧选取，共3989帧，3989帧均有全画面向量；其中3323帧检测到人脸。13条待复核样本的原因均是整条片段未检测到人脸，已设置人脸掩码，其全画面特征仍可用。音频保留原始25 ms窗、10 ms步长的时间索引。压缩特征文件合计19.686 MiB；当前 `outputs/` 总量约27.7 MiB，未包含外部模型缓存。

## 需要人工复核的原始数据与时间

- 题面所述最短时长为2.648 s，但 `-mJ2ud6oKI8$_$6` 与 `-s9qJ7ATP7w$_$6` 的媒体时长分别约2.257 s、2.475 s。工程保留原样并记录提示，没有删改或补长。
- 另有5条样本的视频流声明时长与实际解码帧覆盖时长相差超过0.05 s；详见 `outputs/validation/report.json`。后续聚合以实际解码时间轴为准，同时保留流信息供核查。
- PocketSphinx强制对齐是自动估计。对完整词级、部分词级和片段级样本均须抽听/抽看后才可讨论时间定位准确率；目前通过的是结构合法性检查，不是人工语义核验。
- 13条无人脸样本的逐条ID和原因见 `outputs/stage2/feature_index.csv` 的 `needs_review` 行。可在后续判定为确实无可用人脸、极小人脸、遮挡，或检测器漏检；当前不凭算法结果推断其原因。

一个可核查的同轴例子是 `-3g5yACwYnA$_$13`：首个视频帧从0.00 s开始，首个音频分析窗从0.00 s开始，首个转写词 `They've` 的自动对齐范围为0.00—0.42 s。逐帧、逐词的源时间和索引分别见该样本的 timeline JSON 与 NPZ。此例展示数据结构，不构成人工核验的准确性证据。

## 复跑与验收

在工程根目录执行 README 中的五条命令，最后的 `validate_outputs.py` 应返回退出码0，当前报告为 `errors=0 warnings=7`。额外的两项确定性测试检查200 Hz合成语音的特征/基频，以及5 Hz选帧是否保留源帧号；两项均通过。`--pilot` 可用于阶段1、2的3条试跑，但完整100条结果必须不带该参数生成。

本轮仅完成阶段0—2。后续阶段3仍需按题目要求制定统一50窗聚合、缺失策略与公平比较；不得直接把原始不同长度序列或本工程维度与附件2既有特征混作同一输入。
